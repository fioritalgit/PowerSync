# PowerSync Configuration
replication:
  connections:
    - type: postgresql
      uri: postgresql://postgres:YOUR_PASSWORD@db.pbjpdkvdukiwkcskyanp.supabase.co:5432/postgres?sslmode=require

storage:
  type: postgresql
  uri: postgresql://postgres:YOUR_PASSWORD@db.pbjpdkvdukiwkcskyanp.supabase.co:5432/postgres?sslmode=require

# Disabilita migrazioni automatiche
migrations:
  disable_auto_migration: true

port: 8080

sync_rules:
  content: |
    bucket_definitions:
      global:
        data: []
