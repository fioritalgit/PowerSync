# PowerSync Configuration
# IMPORTANTE: Sostituisci YOUR_PASSWORD con la tua vera password di Supabase
replication:
  connections:
    - type: postgresql
      uri: postgresql://postgres:YOUR_PASSWORD@db.pbjpdkvdukiwkcskyanp.supabase.co:5432/postgres?sslmode=require

storage:
  type: postgresql
  uri: postgresql://postgres:YOUR_PASSWORD@db.pbjpdkvdukiwkcskyanp.supabase.co:5432/postgres?sslmode=require

port: 8080

# Basic sync rules - customize based on your tables
sync_rules:
  content: |
    bucket_definitions:
      global:
        # Add your tables here
        # Example:
        # data:
        #   - SELECT * FROM posts
        #   - SELECT * FROM comments
        data: []
